# Startup integration is a baseline core capability

SSHWarden will include startup installation and removal as part of the baseline cross-platform experience rather than treating it as a later desktop enhancement. A local SSH agent is expected to be available after desktop login, so each supported platform needs a minimal login-session integration: Startup folder shortcut on Windows, LaunchAgent on macOS, and XDG Autostart or equivalent on Linux.
